# Tabla de multiplicar | Ejemplo 2:

# Captura de datos:
numero = eval(input("Ingrese un número: "))

# Ciclo con rango(inicio, final, incremento)
for i in range(0,11,1):
    # i Salidas:
    print(numero, " x ", i, " = ", numero * i )